# 🚀 LOCUS — Полная инструкция по установке и запуску

Эта инструкция поможет вам запустить проект LOCUS с нуля.

---

## 📋 Требования

- **Node.js** 18+ ([скачать](https://nodejs.org/))
- **npm** (устанавливается вместе с Node.js)
- **Аккаунт на Neon** ([регистрация бесплатная](https://neon.tech))

---

## 🔧 Шаг 1: Установка зависимостей

Откройте терминал в корневой папке проекта и выполните:

```bash
npm run install:all
```

Эта команда установит зависимости для:
- Корневого проекта
- Frontend (Next.js)
- Backend (NestJS)

**Ожидаемое время:** 2-5 минут

---

## 🗄️ Шаг 2: Настройка Neon PostgreSQL

⚠️ **ВАЖНО:** Проект использует **ТОЛЬКО** Neon PostgreSQL (облако). Локальный PostgreSQL **не поддерживается**.

### 2.1. Регистрация на Neon

1. Перейдите на [https://neon.tech](https://neon.tech)
2. Нажмите **"Sign Up"** (можно через GitHub)
3. Подтвердите email

### 2.2. Создание проекта

1. После входа нажмите **"Create a project"**
2. Выберите:
   - **Project name:** `locus` (или любое другое)
   - **Region:** ближайший к вам (например, `Europe (Frankfurt)`)
   - **PostgreSQL version:** `15` или `16`
3. Нажмите **"Create project"**

### 2.3. Получение Connection String

1. После создания проекта откроется Dashboard
2. Найдите секцию **"Connection Details"**
3. Скопируйте **Connection String** (формат: `postgresql://user:password@host/dbname?sslmode=require`)

**Пример:**
```
postgresql://user:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require
```

⚠️ **Важно:** Connection String должен содержать `?sslmode=require` в конце.

---

## ⚙️ Шаг 3: Настройка переменных окружения

### 3.1. Создание файла `.env`

В папке `backend/` создайте файл `.env`:

```bash
cd backend
copy .env.example .env
```

Или вручную создайте файл `backend/.env` и скопируйте содержимое из `backend/.env.example`.

### 3.2. Заполнение `.env`

Откройте `backend/.env` и заполните следующие переменные:

⚠️ **КРИТИЧЕСКИ ВАЖНО:** Если в файле уже есть строка `DATABASE_URL=postgresql://...@localhost:5432/...`, **ОБЯЗАТЕЛЬНО замените её** на Connection String из Neon!

```env
# Database Configuration (Neon PostgreSQL)
# Вставьте ваш Connection String из Neon (НЕ localhost!)
DATABASE_URL=postgresql://user:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production-12345
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3001
FRONTEND_URL=http://localhost:3000
NODE_ENV=development

# Telegram Bot Configuration (опционально, для Telegram авторизации)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_BOT_NAME=your_telegram_bot_username

# Email Configuration (опционально, для отправки кодов)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@locus.ru

# SMS Configuration (опционально, для отправки SMS)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Mapbox Configuration (опционально, для карты)
MAPBOX_ACCESS_TOKEN=your-mapbox-token
```

**Минимально необходимые переменные:**
- `DATABASE_URL` — **ОБЯЗАТЕЛЬНО**
- `JWT_SECRET` — **ОБЯЗАТЕЛЬНО**
- Остальные можно оставить по умолчанию или заполнить позже

---

## 🚀 Шаг 4: Запуск проекта

### 4.1. Запуск Frontend и Backend одновременно

В корневой папке проекта выполните:

```bash
npm run dev
```

Эта команда запустит:
- **Frontend** на `http://localhost:3000`
- **Backend** на `http://localhost:3001`

### 4.2. Запуск отдельно

**Только Frontend:**
```bash
npm run dev:frontend
```

**Только Backend:**
```bash
npm run dev:backend
```

---

## ✅ Шаг 5: Проверка работы

### 5.1. Проверка Backend

После запуска в терминале вы должны увидеть:

```
✅ Database connection established (Neon PostgreSQL)
🚀 LOCUS Backend running on http://localhost:3001
📊 Database: Neon PostgreSQL (cloud)
```

Если видите эти сообщения — **Backend работает корректно!**

### 5.2. Проверка Frontend

1. Откройте браузер: `http://localhost:3000`
2. Должна открыться главная страница LOCUS
3. Попробуйте перейти на `/register` — должна открыться страница регистрации

### 5.3. Проверка регистрации

1. Перейдите на `http://localhost:3000/register`
2. Заполните форму регистрации
3. Нажмите **"Зарегистрироваться"**

**Если регистрация прошла успешно:**
- Вы будете перенаправлены на главную страницу
- В консоли браузера не будет ошибок

**Если видите ошибку "Сервер не отвечает":**
- См. раздел **"Решение проблем"** ниже

---

## 🔍 Решение проблем

### ❌ Ошибка: "Local PostgreSQL is not allowed!"

**Причина:** В `DATABASE_URL` указан `localhost` или `127.0.0.1`.

**Решение:**
1. Откройте `backend/.env`
2. Найдите строку `DATABASE_URL=postgresql://...@localhost:5432/...`
3. **Полностью замените** эту строку на Connection String из Neon
4. Формат должен быть: `postgresql://user:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require`
5. **НЕ должно быть** `localhost` или `127.0.0.1` в Connection String!
6. Сохраните файл и перезапустите Backend

### ❌ Ошибка: "DATABASE_URL is not set"

**Причина:** Файл `backend/.env` не создан или переменная `DATABASE_URL` отсутствует.

**Решение:**
1. Создайте файл `backend/.env`
2. Скопируйте содержимое из `backend/.env.example`
3. Заполните `DATABASE_URL` вашим Connection String из Neon

### ❌ Ошибка: "Сервер не отвечает" при регистрации/входе

**Причина:** Backend не запущен или не может подключиться к базе данных.

**Решение:**
1. Проверьте, что Backend запущен (см. раздел "Проверка Backend")
2. Если Backend не запускается:
   - Проверьте `DATABASE_URL` в `backend/.env`
   - Убедитесь, что Connection String содержит `?sslmode=require`
   - Проверьте, что Neon проект активен: [https://console.neon.tech](https://console.neon.tech)
3. Перезапустите Backend:
   ```bash
   npm run dev:backend
   ```

### ❌ Ошибка: "ECONNREFUSED" или "Connection failed"

**Причина:** Неверный Connection String или Neon проект неактивен.

**Решение:**
1. Проверьте Connection String в Neon Dashboard
2. Убедитесь, что проект активен (не приостановлен)
3. Скопируйте Connection String заново и обновите `DATABASE_URL` в `backend/.env`
4. Перезапустите Backend

### ❌ Ошибка: "Port 3001 already in use"

**Причина:** Порт 3001 уже занят другим процессом.

**Решение:**
1. Найдите процесс, использующий порт 3001:
   ```bash
   # Windows PowerShell
   netstat -ano | findstr :3001
   ```
2. Завершите процесс или измените `PORT` в `backend/.env` на другой (например, `3002`)

### ❌ Frontend не открывается

**Причина:** Frontend не запущен или порт 3000 занят.

**Решение:**
1. Проверьте, что Frontend запущен (см. терминал)
2. Если порт 3000 занят, измените порт в `frontend/package.json`:
   ```json
   "dev": "next dev -p 3001"
   ```

---

## 📝 Полезные команды

### Установка зависимостей
```bash
npm run install:all
```

### Запуск разработки (Frontend + Backend)
```bash
npm run dev
```

### Сборка для production
```bash
npm run build
```

### Запуск только Frontend
```bash
npm run dev:frontend
```

### Запуск только Backend
```bash
npm run dev:backend
```

---

## 🎯 Что дальше?

После успешного запуска:

1. ✅ **Регистрация и вход** — проверьте, что регистрация работает
2. ✅ **Telegram авторизация** — настройте Telegram Bot (см. `TELEGRAM_BOT_TOKEN` в `.env`)
3. ✅ **Размещение объявлений** — протестируйте создание объявлений
4. ✅ **Поиск** — проверьте поиск жилья с фильтрами

---

## 📞 Нужна помощь?

Если проблема не решена:

1. Проверьте логи в терминале (Backend и Frontend)
2. Откройте консоль браузера (F12) и проверьте ошибки
3. Убедитесь, что все переменные в `backend/.env` заполнены корректно
4. Проверьте, что Neon проект активен: [https://console.neon.tech](https://console.neon.tech)

---

**Успешного запуска! 🚀**
